CutTrack v61 — Preset Meal Reminders
Built directly from the stable v60 baseline.

Changes only to the Reminders section:
- Breakfast, Lunch and Dinner are permanent preset reminders.
- Presets can be edited and switched on/off.
- Removed the Supplements preset.
- Removed the old Add links and + Custom header action.
- Added one + Add Reminder button at the bottom for custom reminders.
- Existing custom reminders remain listed below the three presets.
- Preset reminders cannot be deleted; custom reminders can still be deleted.

Everything else is unchanged. Storage key remains cuttrack_v9.
Upload all 6 files to the root of the -cuttrack GitHub Pages repo and replace the existing files.
